/* 
CREATE DATABASE kuharica;
use kuharica;


CREATE table recepti(
ID int(11) not null AUTO_INCREMENT primary key,
    ime_umjetnika varchar(60),
    ime_recepta varchar(200),
    sastojci varchar(1000),
    piprema varchar(2000),
    slika varchar(500)
    );

    CREATE TABLE korisnici(
    ID int(11) not null AUTO_INCREMENT primary key,
    ime_korisnika varchar(60),
    email varchar(50),
    pass varchar(1000)
);